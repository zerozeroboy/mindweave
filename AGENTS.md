#  MindWeave 项目规则（AGENTS.md）

本文件用于给 Agent/自动化工具提供项目级约束与工作准则。启用“将 AGENTS.md 包含在上下文中”时，应始终遵守本文件。

## 0. 三条硬规则

1. **永远不向量化**：禁止使用 embedding、向量数据库与任何 RAG 技术路线。
2. **完全面向 Agent 场景**：系统设计与实现以 Agent 可自动化为第一原则；人类只做两件事：提问与保存/整理原始文档（选择目录、放入文件、同步）。
