# MindWeave Todo

## 任务清单

### P1

- [x] 实现实时打断与纠偏（Human-in-the-loop）
  - 背景/问题
    - 针对长任务（如大范围查阅代码或整理长文档），当前 `agent-runtime.ts` 仅有被动的超时熔断（90秒）和轮次限制（12轮）。
    - 缺少实时干预手段，当 Agent 方向错误时，用户只能干等熔断，浪费时间和 Token。
  - 目标
    - 在对话过程中提供明确的“停止并介入”机制，支持切断网络请求和后台循环，节省成本并支持及时纠偏。
  - 范围
    - 前端（UI层）：增加打断按钮交互和状态管理。
    - API层：引入请求级别的可中止信号（如 `AbortController` / SSE 连接断开监听）。
    - 运行时层（`agent-runtime.ts`）：支持传递和响应中止信号（`AbortSignal`），在循环边界与大模型请求层进行熔断。
  - 方案建议
    - 前端发起 `/api/chat` 请求时绑定 `AbortController`，并在用户点击“停止生成”时调用 `abort()` 中断 SSE 流。
    - 后端接口监听客户端断开连接，更新对应对话的取消标志。
    - 运行时 `runAgentChatStream` 的 `while(true)` 循环节点以及调用 `doubao-client` 时均判断中断标志，如果中断则立刻 yield 中止系统提示并平滑退出。
  - 验收标准
    - 在 Agent 连续执行工具期间，用户点击“停止”，Agent 能立刻停止后续工具调用与输出。
    - 输出明确的系统提示（如 `[系统提示：用户已手动终止]`）。
    - 底层调用正常取消，不会产生额外的 API 调用开销。

### P0（高优改进）

- [ ] 实现 `@文件` 强干预能力
  - 背景：当前用户只能输入文字，Agent 需自行判断并搜索文件，往往浪费 Token 和时间。
  - 目标：在底部输入框支持 `@` 唤出当前工作区文件树，直接选择并注入文件内容作为上下文。
  - 价值：免去 Agent 盲目调用 `read_file` 的步骤，缩短摸索路径，大幅提升响应速度和精准度。

- [ ] 增加写操作 Review / Undo 机制
  - 背景：Agent 直接对文件执行 `patch_file` 或 `write_file`，缺乏安全缓冲。
  - 目标：对于高危写操作，UI 弹出包含 Diff 草稿的“确认卡片”，用户点击 Approve 后再落盘；提供全局撤销（Undo）能力。
  - 价值：为 Agent 的修改动作建立明确的信任边界，增强用户安全感和可控性。

- [ ] 建立轻量级目录摘要（心智地图）
  - 背景：坚决不使用 RAG 和向量库，导致无明确文件名的自然语言提问检索效率极低。
  - 目标：同步目录时，低成本为文件生成单行摘要，自动汇总至 `__workspace_map.md`，强制 Agent 优先读取。
  - 价值：通过低成本的“伪语义路由”弥补纯字面量搜索的硬伤，大幅提升盲搜准确率。

### P1（体验优化 & 流程演进）

- [ ] 引入 Todo/Plan 任务拆解模式
  - 目标：面对复杂宏观指令，要求 Agent 优先调用新增的 `create_plan` 工具，在 UI 上生成可交互的 Checklist，逐步执行且允许用户随时介入。
- [ ] 同步 (Sync) 过程体验优化
  - 目标：实现流式/渐进式索引（后台静默解析 PDF，不阻塞聊天）；提供显式的 `.agentignore` 忽略清单入口，防止卡死。
- [ ] Agent 思考过程“降噪”
  - 目标：向普通用户隐藏晦涩的 JSON 参数，UI 上将工具调用包装为自然语言状态（如“🔍 正在查阅 3 个相关文件...”），仅 Hover 展示详情。

### P2（长期商业化与护城河）

- [ ] 插件化与 MCP 协议接入
  - 目标：允许用户自定义 Python/Node 脚本作为自定义 Tool，或连接外部 MCP Server，实现从“文件处理器”向“工作流引擎”跨越。
- [ ] 多模态与视觉能力深度整合
  - 目标：利用现有的图片/视频镜像体系，接入 Vision 视觉模型。支持基于实际画面的问答（如“分析一下这张架构图”），形成降维打击。

