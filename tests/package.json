{
  "name": "agentos-tests",
  "private": true,
  "type": "module",
  "scripts": {
    "sample:fetch-webpage": "npm --prefix .. run build:electron && node fetch-webpage.sample.mjs",
    "test:unit": "npm --prefix .. run build:electron && node --test search-files.unit.mjs"
  }
}
