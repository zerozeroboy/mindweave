{
  "name": "agentos-tests",
  "private": true,
  "type": "module",
  "scripts": {
    "sample:fetch-webpage": "npm --prefix .. run build:electron && node fetch-webpage.sample.mjs",
    "test:unit": "npm --prefix .. run build:electron && node --test search-files.unit.mjs",
    "test:api": "npm --prefix .. run build:electron && node --test api-server.unit.mjs",
    "test:chat-ui": "node --test chat-ui-migration.unit.mjs chat-stream-handling.unit.mjs chat-style-cleanup.unit.mjs chat-empty-task-lazy-create.unit.mjs"
  }
}
